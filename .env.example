# Hugging Face access token (needs write for pushing, read for fetching private repos).
# Create one at: https://huggingface.co/settings/tokens
HF_TOKEN=

# Hugging Face account that owns the dataset repos (used as <owner>/<DATASET>).
HF_OWNER=savoji
